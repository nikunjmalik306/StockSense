# VITE_API_BASE_URL points to the backend URL.
# For local development, it can be http://localhost:8000
# For production, it should be the URL of the deployed backend (e.g., https://api.stocksense.example.com)
VITE_API_BASE_URL=http://localhost:8000
